---
permalink: "/index.json"
---
[
    {% for post in collections.posts %}
    {
        "url": "{{ post.url | url }}",
        "title": "{{ post.data.title }}",
        "templateContent": "{{ post.templateContent | striptags  }}"
    }{% if not loop.last %},{% endif %}
    {% endfor %}
]